{
	"name": "viewfinder",
	"module": "index.ts",
	"devDependencies": {
		"@biomejs/biome": "2.2.2",
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	},
	"scripts": {
		"dev": "DEBUG=true bun run src/index.ts",
		"benchmark": "bun run src/index.ts",
		"server": "bun run websocket-server.ts",
		"format": "biome format --write .",
		"lint": "biome lint .",
		"typecheck": "tsc --noEmit"
	},
	"type": "module"
}
